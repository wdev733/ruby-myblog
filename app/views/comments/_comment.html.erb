
<p> <%= link_to comment.user.username, user_path(comment.user) %>: <%= comment.body %> <sub> <%= time_ago_in_words(comment.created_at)%> ago 
<%= (link_to 'Delete',[comment.article,comment] , method: :delete, confirm: "Are you sure ?", class: "btn btn-danger btn-xs" ) if current_user == comment.user  or current_user.admin? %></sub></p>
